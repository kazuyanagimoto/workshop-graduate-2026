% Anti-pattern: on a 4:3 frame the line is too wide and wraps onto a second
% line. Compare with aspect-good.tex (16:9), where the same sentence fits.
\documentclass{beamer}

\begin{document}

\begin{frame}{Aspect Ratio}
  We estimate the model $y_{it} = \alpha_i + \beta x_{it} + \gamma z_{it} + \varepsilon_{it}$ by ordinary least squares.
\end{frame}

\end{document}
